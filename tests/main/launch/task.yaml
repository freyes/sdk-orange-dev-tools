summary: Launch workshop with ubuntu-dev-tools SDK

prepare: |
  envsubst '${BASE}' <workshop.yaml.in >workshop.yaml
  workshop launch

restore: |
  workshop remove || true

execute: |
  # workshop exec -- ubuntu-dev-tools --version

debug: |
  workshop tasks
